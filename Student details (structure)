#include <stdio.h>
struct student{
    int roll;
    char name[20];
};
int main(){
    struct student s;
    scanf("%d %s",&s.roll,s.name);
    printf("Roll: %d Name: %s", s.roll, s.name);
    return 0;
}

#include <stdio.h>
int main() {
    int n, i;
    float sales[30], total = 0;

    scanf("%d", &n);
    for(i = 0; i < n; i++) {
        scanf("%f", &sales[i]);
        total += sales[i];
    }

    printf("Total Sales = %.2f", total);
    return 0;
}

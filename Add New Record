#include <stdio.h>
struct Item {
    int id;
};

int main() {
    int n, i;
    struct Item it[20];

    scanf("%d", &n);
    for(i = 0; i < n; i++) scanf("%d", &it[i].id);

    printf("Enter new ID: ");
    scanf("%d", &it[n].id);
    n++;

    for(i = 0; i < n; i++) printf("%d ", it[i].id);
    return 0;
}

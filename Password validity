#include <stdio.h>
#include <string.h>
int main() {
    char pass[20];
    printf("Enter password: ");
    scanf("%s", pass);

    if(strlen(pass) >= 6)
        printf("Valid Password");
    else
        printf("Invalid Password");

    return 0;
}

#include <stdio.h>
struct Emp {
    int id;
    char name[20];
};

int main() {
    int n, i, key;
    struct Emp e[10];

    scanf("%d", &n);
    for(i = 0; i < n; i++)
        scanf("%d %s", &e[i].id, e[i].name);

    scanf("%d", &key);
    for(i = 0; i < n; i++) {
        if(e[i].id == key) {
            printf("Name: %s", e[i].name);
            return 0;
        }
    }
    printf("Not Found");
    return 0;
}

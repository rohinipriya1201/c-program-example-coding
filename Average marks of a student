#include <stdio.h>
int main() {
    float m1, m2, m3, avg;
    printf("Enter 3 subject marks: ");
    scanf("%f %f %f", &m1, &m2, &m3);
    avg = (m1 + m2 + m3) / 3;
    printf("Average = %.2f", avg);
    return 0;
}

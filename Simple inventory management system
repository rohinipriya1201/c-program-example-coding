#include <stdio.h>
struct item{
    int id, qty;
    float price;
};
int main(){
    struct item i;
    scanf("%d %d %f",&i.id,&i.qty,&i.price);

    printf("ID:%d Qty:%d Price:%.2f", i.id, i.qty, i.price);
    return 0;
}

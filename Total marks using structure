#include <stdio.h>
struct student{
    int m1,m2,m3,total;
};
int main(){
    struct student s;
    scanf("%d %d %d",&s.m1,&s.m2,&s.m3);
    s.total = s.m1 + s.m2 + s.m3;
    printf("Total = %d", s.total);
    return 0;
}
